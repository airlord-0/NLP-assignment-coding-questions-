# 14. Write a Python function to remove punctuation from text.

# make a function that take "text as input and process it to remove punctuation 
def remove_Punctuation(text) :
  punctuation='''!@#$%^&*(){}|:"?<>,. ₹'''

  clean_text=" "

# if the char in the text is in the punctuation do nothing, if it is not in the punctuation then add it in clean_text

  for char in text :
    if char not in punctuation :
      clean_text += char

  return clean_text

# now give the sample 
sample = "kese ho !, ghar mai sab bhadiya ?, $ chahiye yah ₹ chahiye :)"
print(remove_punctuation(sample))

